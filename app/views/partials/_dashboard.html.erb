<section id="dash">
  <%= render 'partials/flash' %>
  <div class="dash-header">
    <h1><%= breadcrumbs %></h1>
  </div>
  <div class="dashboard">
    <%= render "partials/sidebar" %>
    <div class="content">
      <%= yield %>
    </div>
  </div>
</section>