<section id="dash">
	<%= render 'partials/flash' %>
	<h1>Dashboard</h1>
	<div class="dashboard">
		<%= render "partials/sidebar" %>
		<div class="content">
			<%= yield %>
		</div>
	</div>
</section>
